# AGENTS.md

## 回复约束

```
每次回复必须以 `(∠・ω＜ )⌒☆` 开头。
```

## AI 协作规范

### 工作原则

* 优先理解需求，再实施修改。
* 优先理解现有代码，而非重新设计。
* 优先最小改动，而非大范围重构。
* 保持与现有代码风格一致。
* 禁止未经授权修改无关代码。

---

## 变更策略

### 最小改动原则

仅修改与当前需求直接相关的内容。

禁止：

* 顺手优化
* 顺手重构
* 风格统一
* 大规模格式化
* 修改无关文件

### 影响分析

涉及以下内容时必须说明影响范围：

* 接口变更
* 数据结构变更
* 数据库字段变更
* 配置变更
* 权限变更
* 业务流程变更

---

## 执行流程

### 标准流程

1. 阅读相关代码
2. 分析调用链
3. 评估影响范围
4. 实施修改
5. 验证结果
6. 输出变更说明

### 先读后改

修改前优先阅读：

* Controller
* Service
* Repository / Mapper
* DTO
* VO
* Entity
* 配置文件
* 测试代码

禁止在不了解上下文的情况下直接修改。

---

## 输出要求

每次任务完成后应说明：

### 改动内容

说明修改了哪些文件及逻辑。

### 改动原因

说明为什么这样修改。

### 影响范围

说明可能影响的功能模块。

### 验证方式

说明如何验证修改正确。

### 潜在风险

说明可能存在的问题和边界场景。

---

## 信息不足处理

当需求不明确时：

先提供：

* 默认方案
* 备选方案
* 差异分析

然后提出关键问题。

禁止直接猜测业务逻辑。

---

# 验证与编译策略

## 按需验证原则

编译、测试、启动应用均属于高成本操作。

应根据改动规模评估是否执行验证。

禁止为了保险起见执行无意义编译。

---

## 必须执行编译验证

满足以下任一条件：

* 修改核心业务逻辑
* 修改多个模块
* 新增类或接口
* 修改依赖版本
* 修改配置文件
* 修改数据库访问层
* 修改框架集成代码
* 涉及泛型、反射、AOP
* 修改文件数量 ≥ 5
* 代码改动量 ≥ 500 行

---

## 可跳过编译验证

以下情况默认不执行编译：

* 注释修改
* 文档修改
* JavaDoc 修改
* 日志文案修改
* 常量描述修改
* DTO 字段注释修改
* VO 字段注释修改
* 小范围局部代码调整

---

## 验证结果说明

执行编译时必须说明：

* 使用的命令
* 编译结果
* 是否存在警告
* 是否存在失败项

未执行编译时必须说明：

> 本次修改影响范围有限，未执行编译验证。

---

# 安全与风险控制

## 高风险操作

未经明确授权禁止执行：

* 删除数据库
* 批量删除数据
* 覆盖生产配置
* 强制推送代码
* 修改生产环境参数
* 执行不可逆操作

---

## 敏感信息

禁止输出：

* 密码
* Token
* Secret
* AccessKey
* 身份证号
* 银行卡号
* 手机号完整信息
* 其他敏感凭证

---

## 安全开发规范

### SQL

必须使用参数化查询。

禁止：

* SQL 拼接
* 动态拼接条件产生注入风险

### 日志

必须脱敏。

禁止记录：

* 密码
* Token
* 身份证号
* 银行卡号
* 完整手机号

---

# 数据库规范

## 表结构变更

涉及数据库结构修改时必须提供：

### 升级脚本

新增或修改内容。

### 回滚脚本

回退方案。

### 影响说明

影响范围及风险。

---

## 数据删除

默认原则：

* 优先逻辑删除
* 优先归档
* 谨慎使用物理删除

如需物理删除必须获得明确授权。

---

# 异常处理规范

## 异常设计

业务异常与系统异常分离。

必须：

* 使用统一异常体系
* 使用统一错误码体系

禁止：

* 向前端暴露底层异常
* 吞异常

---

## 全局异常处理

所有异常统一处理。

要求：

* 保留错误日志
* 保留完整堆栈
* 返回统一响应格式

---

# Git 操作规范

## 默认禁止

未经用户明确授权禁止执行：

```bash
git commit
git push
git push --force
git commit --amend
```

### 完成修改后

先提供：

* 改动内容
* 影响范围
* 风险分析
* 验证结果

用户明确允许后才可提交。

---

# 开发规范

## 命名规范

| 类型   | 规范               |
| ---- | ---------------- |
| 类名   | PascalCase       |
| 接口名  | PascalCase       |
| 方法名  | camelCase        |
| 变量名  | camelCase        |
| 常量   | UPPER_SNAKE_CASE |
| 包名   | 全小写              |
| 枚举类型 | PascalCase       |
| 枚举值  | UPPER_SNAKE_CASE |

---

## 分层架构

| 层级                  | 职责   |
| ------------------- | ---- |
| Controller          | 请求处理 |
| Service             | 业务逻辑 |
| Repository / Mapper | 数据访问 |
| Entity              | 实体对象 |
| DTO                 | 请求对象 |
| VO                  | 响应对象 |
| Config              | 配置   |
| Enum                | 枚举   |
| Util                | 工具类  |

---

## 类型安全

* 优先使用具体类型
* 集合必须使用泛型
* 固定选项使用枚举
* 避免使用 Object

---

## 方法设计

遵循：

* 单一职责原则
* 高内聚低耦合
* 可读性优先

避免：

* 超长方法
* 深层嵌套
* 重复代码

---

## 注释规范

必须编写：

* 类注释
* 接口注释
* 复杂业务逻辑注释

避免：

* 无意义注释
* 与代码重复的注释

---

# 日志规范

## 日志级别

| 级别    | 场景   |
| ----- | ---- |
| ERROR | 系统异常 |
| WARN  | 潜在风险 |
| INFO  | 关键业务 |
| DEBUG | 调试信息 |

---

## 日志要求

关键业务流程必须记录必要日志。

日志应具备：

* 可追踪性
* 可排查性
* 可定位性

---

# API 设计规范

## 基本原则

* 接口职责单一
* 命名清晰
* 保持向后兼容

---

## 参数校验

所有外部输入必须校验。

包括：

* 空值
* 长度
* 格式
* 枚举范围
* 权限范围

禁止依赖前端校验。

---

## 响应规范

统一返回格式。

要求：

* 成功响应统一
* 错误响应统一
* 错误码统一管理

---

# 枚举与字典规范

## 枚举

固定选项必须使用枚举。

禁止魔法值。

推荐：

```java
private final String code;
private final String desc;
```

---

## 字典

推荐同时返回：

```java
xxxId
xxxName
```

或：

```java
xxxCode
xxxDesc
```

---

# 金额处理规范

* 金额统一使用 BigDecimal
* 禁止使用 float
* 禁止使用 double
* 明确舍入规则
* 保持精度一致

---

# 时间处理规范

推荐：

```java
LocalDate
LocalDateTime
```

避免：

```java
Date
Calendar
```

统一使用系统时区配置。

---

# Code Review 检查清单

提交前检查：

* 功能是否正确
* 是否影响现有功能
* 是否存在空指针风险
* 是否存在并发问题
* 是否存在性能问题
* 是否存在 SQL 风险
* 是否存在安全风险
* 是否补充必要日志
* 是否补充必要注释
* 是否覆盖异常场景
* 是否提供验证方案

---

# 红线规则

禁止：

* 擅自修改业务逻辑
* 擅自修改数据库结构
* 擅自删除代码
* 擅自提交代码
* 擅自执行不可逆操作
* 泄露敏感信息
* 绕过权限校验
* 绕过参数校验
* 绕过异常处理体系
* 修改无关代码
